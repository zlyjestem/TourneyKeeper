import React from 'react'

const Button = (onClick) => {
    // render() {
    return (
        <div>
            <button onClick={this.props.onclick}>
                Get tourney
        </button>
        </div>
    );
    // }
}

export default Button;